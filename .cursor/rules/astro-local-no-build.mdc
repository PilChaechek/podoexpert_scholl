---
description: Astro local project — не запускать build без явной просьбы
globs: local/**/*
alwaysApply: false
---

# Astro (`local/`)

Не запускай `npm run build` и не предлагай его для проверки изменений: проект на Astro, разработка идёт через dev-сервер, всё подхватывается на лету. Сборку запускай только если пользователь явно попросил.
